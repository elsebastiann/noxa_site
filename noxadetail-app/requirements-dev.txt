# Dependencias para correr los tests. No van en requirements.txt: no las usa la
# app en producción y no tiene sentido instalarlas en el contenedor de Railway.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   python -m pytest tests/ -q
#
# httpx lo usa tests/test_saldos.py. Antes llegaba de rebote como dependencia de
# `anthropic`, pero desde la versión 1.0 anthropic depende de `httpx2`, así que
# en un entorno limpio ya no aparece y la suite COMPLETA no arranca: falla al
# recolectar ese archivo y se lleva por delante todos los demás tests.
-r requirements.txt

pytest>=8.0
httpx>=0.27
