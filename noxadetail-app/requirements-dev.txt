# Dependencias que solo usan los tests. Van aparte de requirements.txt a
# propósito: ese archivo es lo que Railway instala en la imagen de producción, y
# meterle un lector de PDF que nadie usa en runtime engorda el build sin razón.
#
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
# Para poder afirmar sobre el TEXTO de los PDF que genera la app —recibos y
# cotizaciones— y no solo sobre que empiecen por %PDF-. Sin esto, los tests de
# contenido se saltan y un recibo podría imprimir cifras que no debe sin que
# nada falle.
pypdf>=4.0
