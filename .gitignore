# macOS
.DS_Store

# Claude Code tooling (no es contenido del sitio)
.claude/
.agents/
skills-lock.json

# Datos sensibles / privados
leads_pauta_noxa.xlsx

# agenda-detalling.zip: snapshot viejo de antes de la unificación, sin tocar por ahora
agenda-detalling.zip

# Documentos de referencia pesados, no forman parte del sitio
hostiger.zip
catalogo.pdf
Coating Ceramico.pdf
Manual_Operativo_NOXA.pdf

# Respaldos con fecha que graphify deja al reconstruir el grafo. Cada uno es
# una copia completa de graph.json y se crea uno por día: versionarlos infla
# el repo sin aportar nada que el propio grafo o el historial no tengan ya.
**/graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
